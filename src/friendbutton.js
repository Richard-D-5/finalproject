import React, { Component } from "react";

export default function FriendButton(props) {
    console.log("props in friendbutton: ", props);
    const butt

    return <button>Make Friend Request</button>;
}
